import 'package:flutter/material.dart';

class AppColors {

  // Brand
  static const primary = Color(0xFFA27EFF);
  static const secondary = Color(0xFF6A9FFF);
  static const accent = Color(0xFF00D1D1);

  // Backgrounds
  static const background = Color(0xFFF8F7FC);
  static const surface = Colors.white;
  static const surfaceVariant = Color(0xFFF3F0FA);

  // Text
  static const textPrimary = Color(0xFF1F1F29);
  static const textSecondary = Color(0xFF6C757D);

  // Borders
  static const outline = Color(0xFFE5E7EB);

  // States
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFA500);
  static const error = Color(0xFFDC2626);
}